# Local tools

Put repository-local tools or downloaded helper binaries here when a task
explicitly calls for them. The directory is ignored by default so large SDKs
and machine-specific binaries are not committed accidentally.
